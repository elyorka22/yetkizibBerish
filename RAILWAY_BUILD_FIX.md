# 🔧 Исправление проблемы с билдом Railway

## ❌ Проблема

Railway пытается использовать Dockerfile, хотя builder установлен на NIXPACKS, и выдает ошибку:
```
dockerfile parse error on line 12: ENV names can not be blank
```

## ✅ Решение

### Шаг 1: Настройки в Railway Dashboard

1. Откройте Railway Dashboard → Ваш проект → Settings
2. **Root Directory**: оставьте **ПУСТЫМ** (не указывайте `backend`)
3. **Builder**: выберите **"Nixpacks"** (если есть опция)
4. **Build Command**: оставьте **ПУСТЫМ**
5. **Start Command**: оставьте **ПУСТЫМ**

### Шаг 2: Проверьте файлы в репозитории

Убедитесь, что в корне проекта есть:
- ✅ `railway.json` (в корне, не в backend/)
- ✅ `nixpacks.toml` (в корне)
- ✅ `Dockerfile` (пустой файл, чтобы предотвратить автогенерацию)

### Шаг 3: Структура файлов

```
yetkizibBerish/
├── railway.json          ← В корне!
├── nixpacks.toml        ← В корне!
├── Dockerfile           ← Пустой файл в корне
├── backend/
│   ├── package.json
│   ├── src/
│   └── ...
└── frontend/
    └── ...
```

### Шаг 4: Перезапуск деплоя

1. После пуша Railway автоматически перезапустит деплой
2. Или вручную: Deployments → последний деплой → Redeploy

## 🔍 Проверка

После деплоя в логах должно быть:

```
using build driver nixpacks-v1.41.0
╔═══════════════════════ Nixpacks v1.41.0 ══════════════════════╗
║ setup      │ nodejs-18_x                                      ║
║ install    │ cd backend && npm install                         ║
║ build      │ cd backend && npm run build                       ║
║ start      │ cd backend && npm start                          ║
```

**НЕ должно быть:**
```
internal load build definition from Dockerfile
```

## ⚠️ Если проблема сохраняется

### Вариант 1: Удалить и пересоздать сервис

1. В Railway Dashboard удалите текущий сервис
2. Создайте новый сервис
3. Подключите тот же репозиторий
4. **Root Directory**: оставьте пустым
5. Добавьте переменные окружения заново

### Вариант 2: Явно указать nixpacks

В Railway Dashboard:
1. Settings → Build & Deploy
2. Найдите опцию "Build Command"
3. Оставьте пустым или укажите: `nixpacks build .`
4. Сохраните

### Вариант 3: Проверить кэш

1. Settings → Clear Build Cache
2. Перезапустите деплой

## 📝 Важные моменты

- **НЕ** создавайте `Dockerfile` вручную (кроме пустого для предотвращения автогенерации)
- **НЕ** указывайте `backend/` в Root Directory
- **НЕ** указывайте Build Command и Start Command вручную (используются из конфигов)
- **ИСПОЛЬЗУЙТЕ** только `railway.json` и `nixpacks.toml` в корне

## ✅ После исправления

1. Деплой должен использовать nixpacks
2. Сборка должна пройти успешно
3. Сервер должен запуститься на порту 3001
4. Health endpoint должен работать: `/health`

