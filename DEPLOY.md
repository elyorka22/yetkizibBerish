# 🚀 Руководство по деплою

## 📋 Подготовка к деплою

### ✅ Чек-лист перед деплоем

- [ ] Firebase проект создан и настроен
- [ ] Authentication включен (Email/Password)
- [ ] Firestore Database создана
- [ ] Security Rules установлены
- [ ] Service Account создан для backend
- [ ] Первый пользователь создан
- [ ] Все переменные окружения готовы

## 🌐 Frontend - Деплой на Vercel

### Шаг 1: Подготовка репозитория

1. Убедитесь, что код закоммичен и запушен в Git:
```bash
git add .
git commit -m "Prepare for deployment"
git push origin main
```

### Шаг 2: Создание проекта в Vercel

1. Откройте [Vercel](https://vercel.com/)
2. Войдите или зарегистрируйтесь
3. Нажмите **"Add New Project"**
4. Подключите ваш Git репозиторий
5. Выберите репозиторий с проектом

### Шаг 3: Настройка проекта

**Настройки проекта:**
- **Framework Preset:** Next.js
- **Root Directory:** `frontend`
- **Build Command:** `npm run build` (автоматически)
- **Output Directory:** `.next` (автоматически)
- **Install Command:** `npm install` (автоматически)

### Шаг 4: Переменные окружения

**Вариант 1: Одна переменная с JSON (рекомендуется)**

Добавьте одну переменную окружения в Vercel:

```
NEXT_PUBLIC_FIREBASE_CONFIG={"apiKey":"AIzaSyBdc-5mE9b1zy-pjSKfMCEnmyrWlfNOEtg","authDomain":"yetkazibberish-c5af5.firebaseapp.com","projectId":"yetkazibberish-c5af5","storageBucket":"yetkazibberish-c5af5.firebasestorage.app","messagingSenderId":"258709998480","appId":"1:258709998480:web:293cd08d2905ab90414115"}
NEXT_PUBLIC_API_URL=https://your-backend-url.railway.app
```

**Вариант 2: Отдельные переменные (альтернатива)**

Если предпочитаете отдельные переменные:

```
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyBdc-5mE9b1zy-pjSKfMCEnmyrWlfNOEtg
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=yetkazibberish-c5af5.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=yetkazibberish-c5af5
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=yetkazibberish-c5af5.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=258709998480
NEXT_PUBLIC_FIREBASE_APP_ID=1:258709998480:web:293cd08d2905ab90414115
NEXT_PUBLIC_API_URL=https://your-backend-url.railway.app
```

**Важно:** 
- Замените `https://your-backend-url.railway.app` на реальный URL вашего backend после деплоя на Railway
- При использовании JSON варианта убедитесь, что JSON в одну строку без переносов

### Шаг 5: Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения сборки
3. После успешного деплоя вы получите URL вида: `https://your-project.vercel.app`

## 🚂 Backend - Деплой на Railway

### Шаг 1: Создание Service Account

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект: **yetkazibberish-c5af5**
3. Перейдите в **Project Settings** (⚙️) → **Service Accounts**
4. Нажмите **"Generate new private key"** (Создать новый закрытый ключ)
5. Подтвердите создание ключа
6. JSON файл автоматически скачается

**📖 Подробная инструкция:** См. [HOW_TO_GET_FIREBASE_PRIVATE_KEY.md](./HOW_TO_GET_FIREBASE_PRIVATE_KEY.md)

### Шаг 2: Создание проекта в Railway

1. Откройте [Railway](https://railway.app/)
2. Войдите или зарегистрируйтесь
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Подключите ваш репозиторий

### Шаг 3: Настройка проекта

**Настройки:**
- **Root Directory:** `backend`
- **Build Command:** автоматически определится
- **Start Command:** `npm start`

### Шаг 4: Переменные окружения

Добавьте следующие переменные окружения в Railway:

```
PORT=3001
FIREBASE_PROJECT_ID=yetkazibberish-c5af5
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nВАШ_ПРИВАТНЫЙ_КЛЮЧ_ИЗ_JSON\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@yetkazibberish-c5af5.iam.gserviceaccount.com
NODE_ENV=production
```

**Важно:** 
- `FIREBASE_PRIVATE_KEY` должен быть в формате с `\n` (новые строки)
- Скопируйте значение `private_key` из JSON файла **как есть**, включая `-----BEGIN PRIVATE KEY-----` и `-----END PRIVATE KEY-----`
- `FIREBASE_CLIENT_EMAIL` - это `client_email` из JSON файла
- `FIREBASE_PROJECT_ID` - это `project_id` из JSON файла

**📖 Подробная инструкция по получению ключей:** См. [HOW_TO_GET_FIREBASE_PRIVATE_KEY.md](./HOW_TO_GET_FIREBASE_PRIVATE_KEY.md)

### Шаг 5: Деплой

1. Railway автоматически начнет сборку
2. Дождитесь завершения
3. Получите URL вашего API (Railway автоматически создаст домен)

### Шаг 6: Обновление переменных в Vercel

После получения URL backend:

1. Вернитесь в Vercel
2. Перейдите в **Settings** → **Environment Variables**
3. Обновите `NEXT_PUBLIC_API_URL` на реальный URL Railway
4. Перезапустите деплой (Redeploy)

## 🔐 Настройка Firebase для Production

### 1. Authorized Domains

1. Откройте Firebase Console
2. Перейдите в **Authentication** → **Settings** → **Authorized domains**
3. Добавьте домен Vercel (например: `your-project.vercel.app`)
4. Добавьте ваш кастомный домен (если есть)

### 2. Security Rules

Убедитесь, что Security Rules установлены (см. `FIREBASE_RULES.md`)

### 3. CORS настройки (если нужно)

Если возникают проблемы с CORS, добавьте в `next.config.js`:

```javascript
async headers() {
  return [
    {
      source: '/api/:path*',
      headers: [
        { key: 'Access-Control-Allow-Origin', value: '*' },
      ],
    },
  ];
}
```

## ✅ Проверка после деплоя

### Frontend:
- [ ] Сайт открывается
- [ ] Страница входа работает
- [ ] Каталог товаров загружается
- [ ] Корзина работает
- [ ] Оформление заказа работает

### Backend:
- [ ] Health check работает: `https://your-backend.railway.app/health`
- [ ] API отвечает на запросы
- [ ] Аутентификация работает

### Интеграция:
- [ ] Frontend может обращаться к backend API
- [ ] Заказы создаются корректно
- [ ] Real-time обновления работают

## 🔄 Обновление после деплоя

### Frontend (Vercel):
- Просто запушьте изменения в Git
- Vercel автоматически задеплоит новую версию

### Backend (Railway):
- Запушьте изменения в Git
- Railway автоматически перезапустит сервис

## 📝 Полезные команды

### Локальная проверка production build:

**Frontend:**
```bash
cd frontend
npm run build
npm start
```

**Backend:**
```bash
cd backend
npm run build
npm start
```

## 🐛 Решение проблем

### Frontend не подключается к Firebase
- Проверьте переменные окружения в Vercel
- Убедитесь, что домен добавлен в Authorized domains

### Backend не работает
- Проверьте логи в Railway
- Убедитесь, что все переменные окружения установлены
- Проверьте формат FIREBASE_PRIVATE_KEY (должны быть \n)

### CORS ошибки
- Проверьте настройки CORS в backend
- Убедитесь, что NEXT_PUBLIC_API_URL правильный

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Vercel и Railway
2. Проверьте консоль браузера
3. Убедитесь, что все сервисы Firebase включены

