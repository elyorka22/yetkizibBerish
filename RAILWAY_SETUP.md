# 🚂 Настройка Railway для деплоя Backend

## ⚠️ Важно: Root Directory

В Railway Dashboard **ОБЯЗАТЕЛЬНО** установите:

**Settings → Root Directory:** `backend`

Это критически важно! Без этого Railway будет пытаться запускать команды из корня репозитория, а не из директории `backend`.

## 📋 Шаги настройки

### 1. Создание проекта в Railway

1. Откройте [Railway Dashboard](https://railway.app/dashboard)
2. Создайте новый проект
3. Подключите GitHub репозиторий: `https://github.com/elyorka22/yetkizibBerish.git`

### 2. Настройка Root Directory

1. Перейдите в **Settings** вашего сервиса
2. Найдите раздел **Root Directory**
3. Установите значение: `backend`
4. Сохраните изменения

### 3. Переменные окружения

Добавьте следующие переменные в **Variables**:

```
PORT=3001
FIREBASE_PROJECT_ID=yetkazibberish-c5af5
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nВАШ_ПРИВАТНЫЙ_КЛЮЧ\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@yetkazibberish-c5af5.iam.gserviceaccount.com
NODE_ENV=production
```

**Важно:** 
- `FIREBASE_PRIVATE_KEY` должен быть в формате с `\n` (новые строки)
- Скопируйте значение из JSON файла Service Account
- `FIREBASE_CLIENT_EMAIL` - это `client_email` из JSON файла

### 4. Получение Service Account

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Перейдите в **Project Settings** → **Service Accounts**
3. Нажмите **"Generate new private key"**
4. Сохраните JSON файл
5. Скопируйте значения:
   - `private_key` → `FIREBASE_PRIVATE_KEY` (с `\n`)
   - `client_email` → `FIREBASE_CLIENT_EMAIL`
   - `project_id` → `FIREBASE_PROJECT_ID`

### 5. Деплой

После настройки:
1. Railway автоматически начнет сборку
2. Дождитесь завершения
3. Получите URL вашего API

## 🔍 Проверка работы

После успешного деплоя:

1. Откройте URL вашего API
2. Проверьте health endpoint: `https://your-api.railway.app/health`
3. Должен вернуться: `{"status":"ok","timestamp":"..."}`

## 🐛 Решение проблем

### Ошибка: "Missing script: build" или "Missing script: start"

**Причина:** Root Directory не установлен на `backend`

**Решение:**
1. Перейдите в Settings → Root Directory
2. Установите `backend`
3. Перезапустите деплой

### Ошибка: "Cannot find module"

**Причина:** Зависимости не установлены или сборка не выполнена

**Решение:**
1. Проверьте логи сборки
2. Убедитесь, что `npm ci` выполнился успешно
3. Убедитесь, что `npm run build` выполнился успешно

### Ошибка: "Firebase Admin credentials are missing"

**Причина:** Переменные окружения не заданы или неправильно отформатированы

**Решение:**
1. Проверьте все переменные окружения
2. Убедитесь, что `FIREBASE_PRIVATE_KEY` содержит `\n` для переносов строк
3. Убедитесь, что нет лишних пробелов или кавычек

## 📝 Структура проекта

```
yetkizibBerish/
├── backend/          ← Root Directory должен быть здесь
│   ├── package.json
│   ├── railway.json
│   ├── src/
│   └── ...
├── frontend/
└── ...
```

## ✅ Чек-лист

- [ ] Root Directory установлен на `backend`
- [ ] Все переменные окружения добавлены
- [ ] Service Account создан в Firebase
- [ ] `FIREBASE_PRIVATE_KEY` правильно отформатирован (с `\n`)
- [ ] Деплой успешно завершен
- [ ] Health endpoint отвечает

