# ⚡ Быстрая настройка Firebase (5 минут)

## 🎯 Цель
Подключить Firebase для работы авторизации и базы данных.

## 📝 Шаги

### 1. Создайте проект Firebase (2 минуты)

1. Откройте https://console.firebase.google.com/
2. Нажмите **"Add project"** или **"Создать проект"**
3. Введите название: `yetkazib-beish` (или любое другое)
4. Нажмите **"Continue"** → **"Continue"** → **"Create project"**
5. Дождитесь создания (30 секунд)

### 2. Включите Authentication (1 минута)

1. В левом меню выберите **Authentication**
2. Нажмите **"Get started"**
3. Перейдите на вкладку **"Sign-in method"**
4. Нажмите на **"Email/Password"**
5. Включите переключатель
6. Нажмите **"Save"**

### 3. Создайте Firestore (1 минута)

1. В левом меню выберите **Firestore Database**
2. Нажмите **"Create database"**
3. Выберите **"Start in test mode"** (для разработки)
4. Выберите регион (например: `us-central`)
5. Нажмите **"Enable"**

### 4. Получите конфигурацию (30 секунд)

1. Нажмите на ⚙️ (шестеренку) вверху → **"Project settings"**
2. Прокрутите вниз до **"Your apps"**
3. Нажмите на иконку **Web** (`</>`)
4. Введите название: `YetkazibBeish Web`
5. Нажмите **"Register app"**
6. **Скопируйте конфигурацию** (она выглядит так):

```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abc123"
};
```

### 5. Обновите .env.local (30 секунд)

1. Откройте файл: `frontend/.env.local`
2. Замените значения на реальные из Firebase:

```env
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSy... (ваш apiKey)
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abc123
NEXT_PUBLIC_API_URL=http://localhost:3001
```

3. Сохраните файл

### 6. Перезапустите сервер (10 секунд)

```bash
# Остановите сервер (Ctrl+C в терминале)
# Затем запустите снова:
cd frontend
npm run dev
```

### 7. Создайте первого пользователя (1 минута)

1. В Firebase Console → **Authentication** → **Users**
2. Нажмите **"Add user"**
3. Email: `admin@demo.com`
4. Пароль: `admin123`
5. Нажмите **"Add user"**
6. **Скопируйте UID** (длинная строка)

### 8. Создайте запись в Firestore (30 секунд)

1. В Firebase Console → **Firestore Database**
2. Нажмите **"Start collection"**
3. Collection ID: `users`
4. Document ID: вставьте **UID** из шага 7
5. Добавьте поля:

| Field | Type | Value |
|-------|------|-------|
| email | string | admin@demo.com |
| name | string | Admin User |
| phone | string | +998901234567 |
| role | string | super_admin |
| createdAt | timestamp | (нажмите сейчас) |
| updatedAt | timestamp | (нажмите сейчас) |

6. Нажмите **"Save"**

## ✅ Готово!

Теперь вы можете:
1. Открыть http://localhost:3000/login
2. Войти с `admin@demo.com` / `admin123`
3. Всё должно работать!

## 🐛 Если не работает

1. Проверьте, что все переменные в `.env.local` правильные
2. Убедитесь, что перезапустили сервер
3. Проверьте консоль браузера (F12) на ошибки
4. Убедитесь, что Email/Password включен в Authentication

## 📚 Подробная инструкция

См. файл `FIREBASE_SETUP.md` для детальной информации.

